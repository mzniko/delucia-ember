{{#if isEditing}}
  {{partial 'customers/edit'}}
  <button {{action 'doneEditing'}}>Done</button>
{{else}}
  <button {{action 'edit'}}>Edit</button>
{{/if}}

<ul class="customers_list">
  <li>Name: {{name}}</li>
  <li>Email: {{email}}</li>
</ul>

